//ver si queda aca, o en otro archivo de config especifico para debug, y seprar un poco las cosas.
export const DEBUG_STATE_COLORS = {
    walking: 0xffffff,
    queue: 0x00ff00
}

export const DEBUG_OPTIONS = {
    enabled: true,

    colliders: true,
    triggers: true,
    playerHitbox: true,
    npcHitboxes: true,
    npcState: true,
}